While win32 is mentioned in various places, that would require some
cleanup of places we hardcode 64bit toolchain paths.

Eventually I do intend to setup a toolchain to target builds for
win32 as well, but I won't be able to test them at all.

Win64 builds i can at least test a little before uploading.

Before first build:


XXX: Insert windows pre-req instructions
XXX: Insert windows build instructions
XXX: Insert windows test instructions
XXX: Insert windows configuraiton instructions

Consult README.txt as well
