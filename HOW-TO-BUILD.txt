# Clone the repo
[ ! -d rustyrig-fw ] && git clone http://github.com/pripyatautomations/rustyrig-fw --depth=1

cd rustyrig-fw
git submodule init
git submodule update --depth=1
./tools/gen-selfsigned-tls-certs


# XXX: make http and mqtt secrets

#
cd ..
