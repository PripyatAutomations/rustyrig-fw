[general]
* Fix the color parser to not mangle json
  gtk_colorize_string()

* make @username auto-complete once enough unique is typed
  - save last typed before completing so backspace will remove the completed part
    - continue completion only if user presses tab
  - pressing tab at any point should list available username completions
    dont do it with ui_print, make it a little popup like phones do

* Fix/finish server picker & server editor and put them to use
  - We should be able to run without a config file and actually be usable
    - We need to finish the config tab and config saving

* Add ^C, etc for embedding mIRC-style colors

* Fix userlist window
  - window opened at startup doesnt track user changes
  - new windows do not have modal property they should per config

* Implement /names to show who's in the chat (espc in TUI)

* tui: should probably treat status window as the syslog in gtk; make windows 2+ show rig controls

* Set window title to $user@$rig

* Make hotkeys context (tab/window) sensitive

* Support for connecting to multiple servers

* Populate config tab from defconfig, showing radio/text field/etc based on default value check

* Make PTT button stay gray if you don't have TX privileges or are a noob without approval

- Add alt-q exit hotkey?

* Fix PTT button needing clicked twice after TOT fired (if still applies)
  - Make sure TOT expire goes ORANGE

* Support PTT lock; controlled by ctrl or shift clicking PTT button (disable ptt lock by default)

* Add mute/unmute button for RX audio which halts sending the samples entirely

* Implement rest of controls typically used by hams

* Finish reason dialog

* Add a start/stop RX audio button for saving bw isntead of just muting
  - or maybe send the mute/unmute to server who will start/stop sending samples
    as well as sending a start/stop message so gstreamer can be paused

* Fix userlist updates so joins/quits properly update it

* Make VFO widgets able to undock/float

* C4FM mode / dialog

* strip of band buttons at the bottom of vfo populated with the bands
  the radio supports & recall the last frequency on that band, maybe 
  move to the parts of the band based on the mode too.

[bluetooth]	LOW PRIORITY
* Support bluetooth PTT buttons
* Maybe automate routing of headsets when connected, if reasonably feasible?

[cfgedit]	URGENT
! Make save config moves the file to .old first
* Add a button in config editor to replace the config editor contents with 
  in-memory settings instead of config file values

[TUI]
* Add a tab for showing client / radio log (togglable)
