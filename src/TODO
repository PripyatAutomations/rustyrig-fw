[buildconf]
* Support for -i 1.2 style option to allow using an older version's
  eeprom_{layout,types}.json to import data for saving in current version

[Network]
    net/ip4 and net/ip6 split
    net/prefer_ip4
    net/ignore_icmp_echo
    net/ignore_mcast
    net/ignore_bcast

[HostInterface]

[Backplane]

[CAT]
* Implement control over unix pipes in posix mode
* Implement control over stm32 serial over usb
