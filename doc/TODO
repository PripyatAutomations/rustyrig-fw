Immediate needs
---------------
* Elmer/noob mode needs a dialog confirmation system
  - Noob requests elevated privs and is cautioned about their responsibility
  - Elmers are prompted and one can accept responsibility for the user

* Support for multiple rigs/channels

* Anywhere with a bool return type needs to ensure
  that it returns true on success, false on failure
  - currently we still use 0 for success, which is incorrect

* Integrate callsign-lookup in rrserver
  - rrclient sends /qrz $call or /fcc $call to server
  - server spawns callsign-lookup and scrapes output
  - sends to client
  - client displays it to user

Lets make sure unauthenticated users can only use auth commands like login or pass.
Don't allow unauthenticated clients to send pings. That could be ugly.
