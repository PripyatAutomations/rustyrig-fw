Immediate needs
---------------
*: Anywhere libev or mongoose code exists, make sure the other does as well
  - will need to use wslay/mbedtls to accomplish libev support for websocket!
  - IRC code needs updated to use mongoose
*: Make eeprom and cfg_get keys consistent somehow and cfg override the eeprom?
*: Need to fully use the event system so we can avoid lib* calling back directly into program code
*: Make all events contain JSON data
   - Document all the messages
rrserver: move auth stuff (user/privs/etc) to sqlite on posix host
  - on non-posix hosts, these should be compacted and stored in eeprom fields
rrclient: tui should probably treat status window as the syslog in gtk; make window 2+ rig views
www: Fix userlist to show all users not just self + users joined later
librustyaxe: Merge cfg_get_* and eeprom_get_* so we have just cfg_get_* 
  - we should just load the whole eeprom into the config dict at startup???
  - deal with debug.* vs log.* keys that need merged
librustyaxe: Make modules able to register their event handlers on loading
librustyaxe: Ensure modules are completely freed and unloaded. Use dicts or memblocks for internal storage
librustyaxe: Add memblocks, simple allocator who maintains small allocations (replace malloc,realloc,free,etc) with page tracking for release on module unload
librrprotocol: IRC protocol will be used to link sites, with large messages enabled (16k)
rrclient: Add auto-reconnect with back off like webui has
rrclient: Set window title to $user@$rig
rrclient: Make hotkeys context (tab/window) sensitive

------------
- Critical -
------------
* Generate inc/build_config.h from ${PROFILE}.config.json
  - probably in pack-eeprom
  - Maybe split tools/pack-eeprom into pack-eeprom and generate-headers

=================
= librrprotocol =
=================
* track rx bytes & tx bytes in the session
	- all
	- text, media (binary)

===============
= librustyaxe =
===============
[CAT]
* Implement control over unix pipes or a file in posix mode
* Implement control over stm32 serial over usb
* Merge rr_cat_cmd and CATCommandTable

[logger]
* Add support for masking out messages such as '-*.noisy'
? Add support for rate limiting messages by subsys major (first dot)

[config]
* Make config exporter put a comment with the description and default value, if found in defconfig, for each key saved.

[Porting]
minimal inet_ntop() for non-posix

============
= Protocol =
============
* Implement ALERT message that pops up for all users (for shutdown, overheat, SWR alerts).
* User management
  - Add/edit users
  - Reset password
  - User change own password
* CW-U and CW-L
   - CW is centered on the CW dial freq; CW-L is center and width below; CW-R is center and width above
