
===============
= librustyaxe =
===============
* Finish theme support

* IRC
  - handle failed connections/nick in use/other common signon errors gracefully
  - make things less coupled to the tui, so we can chose which ui to load
  - Implement per-channel user lists, so we can populate the tab completion
  - For QUITs, send them to all channel windows that the user is a member of
  - Finish client support
  - Start on server support
  - Use char *irc_to_tui_colors(const char *in);
  * Add a mechanism for prompting for and trying an alternate nick or at least generate one

* rework Log() and tui_print_win() a little so there's a variant i can
    call with a va_list, so i can make a function that blorps into the
    user interface as well as the log file

* TUI: Prevent scrolling of the buffer when content is added, if not at the bottom

